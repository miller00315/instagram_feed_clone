import styled from 'styled-components/native';

export const Small = styled.ImageBackground`
  width: 100%;
  background: #eee;
  aspect-ratio: ${(props) => props.aspect};
`;

export const original = styled.ImageBackground``;
